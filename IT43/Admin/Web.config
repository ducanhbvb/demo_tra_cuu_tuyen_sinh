<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.web>
    <authorization>
      <!-- Admin: toàn quyền, Moderator: quản lý nội dung, TuVanVien: phản hồi tư vấn -->
      <allow roles="Admin,Moderator,TuVanVien" />
      <deny users="*" />
    </authorization>
  </system.web>

  <!-- Cho phép static files (CSS, JS, images) không bị chặn bởi authorization -->
  <location path="Admin.css">
    <system.web>
      <authorization>
        <allow users="*" />
      </authorization>
    </system.web>
  </location>
</configuration>
